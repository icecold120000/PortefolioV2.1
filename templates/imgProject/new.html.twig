{% extends 'base.html.twig' %}

{% block title %}Ajouter une nouvelle image{% endblock %}

{% block body %}
    <h1>Ajouter une nouvelle image</h1>
    <div id="formProjet">
        {{ include('imgProject/_form.html.twig')}}
    </div>

    <div>
        <a href="{{ path('imgProjet_index') }}">
            <button class="btn btn-secondary" id="btnRetour">
                Revenir dans la liste
            </button>
        </a>
    </div>

{% endblock %}